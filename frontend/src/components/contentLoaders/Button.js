import React from 'react'

const Button = props => (
  <div
    className='content-loader button-container button-content-loader'
    style={{
      // follow custom_overrides.sass
      height: '40px',
      minWidth: '150px'
    }}/>
)

export { Button }