import React from 'react'

const SidebarOption = props => (
  <div
    className='content-loader sidebar-option-loader'
    style={{
      // follow .sidebar-option
      height: '1rem',
      paddingVertical: '1rem',
    }}/>
)

export { SidebarOption }